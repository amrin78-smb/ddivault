.env.local
frontend/.env.local
node_modules/
frontend/node_modules/
frontend/.next/
logs/

# TypeScript incremental build info
frontend/*.tsbuildinfo

# packaging archives (build/distribution output, never source)
*.zip

# Claude Code web UI attachments + scratch screenshots (not source)
.claude-web-images/
.claude-web-files/
dns_insights.png

# local demo-seed script (superseded by the bundled suite seeder in netvault/installer/seeds)
frontend/demo-seed.js
demo-seed.js
